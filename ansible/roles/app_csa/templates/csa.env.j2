# csa deployment config (gitignored). Managed by Ansible (app_csa role).
# CSA_PORT selects the published host port; compose maps ${CSA_PORT:-8000}:8000.
{% for key, value in (csa_env | dictsort) %}
{{ key }}={{ value }}
{% endfor %}
