---
galaxy_info:
  author: intergalactic
  description: Internal DNS server using CoreDNS for private subdomain resolution
  license: MIT
  min_ansible_version: "2.9"
  platforms:
    - name: Debian
      versions:
        - all
  galaxy_tags:
    - dns
    - coredns
    - tailscale
    - network

dependencies:
  - role: tailscale
    when: enable_tailscale | default(false)
  - role: docker_host
    when: enable_docker | default(false)
