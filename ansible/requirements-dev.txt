# Development dependencies for Ansible project
# Install with: pip install -r ansible/requirements-dev.txt

# Core Ansible (should match requirements.txt)
ansible==10.5.0
PyYAML==6.0.2

# Linting and code quality
ansible-lint>=6.22.0
yamllint>=1.35.0

# Testing
molecule>=5.0.0
molecule-plugins[docker]>=2.0.0
testinfra>=9.0.0
pytest>=7.0.0

# Pre-commit
pre-commit>=3.0.0
